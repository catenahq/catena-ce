---
# Validation tasks for bootstrap/roles/storage. Replaces tests/goss/storage.yaml.
- name: "validate/storage: data subdirs exist with correct owner/group/mode"
  ansible.builtin.include_role:
    name: common
    tasks_from: _assert_path_perms
  vars:
    _assert_paths:
      - { path: /srv/catena, type: directory, owner: root, group: root, mode: "0755" }
      - { path: /srv/catena/apps, type: directory, owner: ops, group: ops, mode: "0755" }
      - { path: /srv/catena/backup-staging, type: directory, owner: ops, group: ops, mode: "0750" }
      - { path: /srv/catena/docker, type: directory, owner: root, group: root, mode: "0710" }
